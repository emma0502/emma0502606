\input{./._relpath-to-latexroot.ltx}
\documentclass[\latexroot/\projectname]{subfiles}
\input{\latexroot/@resources/subfile-setup.ltx}
\appendix\setcounter{section}{0} % standalone: use appendix numbering

\begin{document}

\FloatBarrier
\section{Details of the HANK and SAM Model}
\whenintegrated{\label{sec:appendix-hank}} % integrated doc: SST for labels

This appendix is a placeholder. The final goods producer uses a CES production function:
\[
  Y_{t} = {\left(\int_{0}^{1} Y_{jt}^{\frac{\epsilon_{p}-1}{\epsilon_{p}}}\, dj\right)}^{\frac{\epsilon_{p}}{\epsilon_{p}-1}},
\]
where $\epsilon_{p}$ is the elasticity of substitution.

% Smart bibliography: Only include bibliography if standalone AND has citations
\smartbib

\end{document}
